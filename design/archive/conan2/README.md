# Archived Conan 2 design

These files describe the superseded `v3.x_conan2` experiment. They are kept as
historical design evidence and are not the contract for `v3.x_spack`.

The active Tula component contract is
[`../../COMPONENTS.md`](../../COMPONENTS.md). The system-level Spack design,
test evidence, support matrix, and technical deck live in the sibling
`tula_cmake/design/` directory.
